<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.5</version>
    <relativePath/>
  </parent>

  <groupId>org.tavall.ai</groupId>
  <artifactId>tavall-ai-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Tavall AI</name>
  <description>MCP-first control plane for cross-project agent tasks and prompt memory.</description>

  <modules>
    <module>tavall-ai-core</module>
    <module>tavall-ai-clean-java-mcp</module>
    <module>tavall-ai-clean-java-harness</module>
    <module>tavall-ai-artifact-tools</module>
    <module>tavall-ai-cache-tools</module>
    <module>tavall-ai-context-tools</module>
    <module>tavall-ai-computer-use-tools</module>
    <module>tavall-ai-orchestration-tools</module>
    <module>tavall-ai-repo-tools</module>
    <module>tavall-ai-validation-tools</module>
    <module>tavall-ai-vector-memory-tools</module>
    <module>tavall-ai-app</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <archunit.version>1.4.1</archunit.version>
    <google.genai.version>1.37.0</google.genai.version>
    <mcp.sdk.version>1.0.0</mcp.sdk.version>
    <mongodb.driver.version>5.6.4</mongodb.driver.version>
    <spoon.version>11.3.0</spoon.version>
    <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
    <pmd.plugin.version>3.26.0</pmd.plugin.version>
    <error.prone.version>2.48.0</error.prone.version>
    <plexus.errorprone.version>2.15.0</plexus.errorprone.version>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-checkstyle-plugin</artifactId>
          <version>${checkstyle.plugin.version}</version>
          <configuration>
            <configLocation>${maven.multiModuleProjectDirectory}/config/lint/checkstyle.xml</configLocation>
            <includeTestSourceDirectory>true</includeTestSourceDirectory>
            <consoleOutput>false</consoleOutput>
            <failOnViolation>false</failOnViolation>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-pmd-plugin</artifactId>
          <version>${pmd.plugin.version}</version>
          <configuration>
            <rulesets>
              <ruleset>${maven.multiModuleProjectDirectory}/config/lint/pmd-ruleset.xml</ruleset>
            </rulesets>
            <includeTests>true</includeTests>
            <failOnViolation>false</failOnViolation>
            <targetJdk>${java.version}</targetJdk>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${java.version}</release>
          <compilerArgs>
            <arg>--enable-preview</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <forkCount>1</forkCount>
          <reuseForks>false</reuseForks>
          <argLine>--enable-preview</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>errorprone-lint</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
              <fork>true</fork>
              <showWarnings>true</showWarnings>
              <compilerId>javac-with-errorprone</compilerId>
              <forceJavacCompilerUse>true</forceJavacCompilerUse>
              <compilerArgs>
                <arg>--enable-preview</arg>
                <arg>-XDcompilePolicy=simple</arg>
                <arg>-Xplugin:ErrorProne -XepDisableAllChecks -Xep:EqualsHashCode:ERROR -Xep:MissingOverride:ERROR -Xep:ArrayEquals:ERROR</arg>
              </compilerArgs>
            </configuration>
            <dependencies>
              <dependency>
                <groupId>org.codehaus.plexus</groupId>
                <artifactId>plexus-compiler-javac-errorprone</artifactId>
                <version>${plexus.errorprone.version}</version>
              </dependency>
              <dependency>
                <groupId>com.google.errorprone</groupId>
                <artifactId>error_prone_core</artifactId>
                <version>${error.prone.version}</version>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
